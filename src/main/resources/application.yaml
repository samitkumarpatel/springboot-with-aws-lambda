spring:
  application:
    name: springboot-with-aws-lambda
  security:
    user:
      name: user
      password: password
      roles: USER
  cloud:
    aws:
      region:
        static: eu-north-1
  main:
    lazy-initialization: true

logging:
  level:
    org.springframework.security: WARN
    web: WARN